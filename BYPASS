local function destroyAntiCheat()
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local StarterGui = game:GetService("StarterGui")
local deletedCount = 0

    -- Combined and expanded target list
    local targetNames = {
        -- Identifiers from Script 1
        "Character", "StarterCharacter", "HdAdminSetup", "AntiCheat", "AntiExploit",
        "Client", "Security", "AC_", "Anti_", "Secure", "Validator", "Detector",
        "Checker", "Monitor", "Watchdog", "Guardian", "Shield", "Protection",
        -- Identifiers from Script 2
        "HDAdminHDClient", "Quantity", "Loader", "Unknown",
        "iLegacy", "HDAdminStarterCharacter"
    }

    -- Optimized scanning function with pcall safety
    local function scanAndDestroy(parent)
        if not parent then return end
        local children = parent:GetChildren()
        for _, obj in ipairs(children) do
            for _, name in ipairs(targetNames) do
                local success, err = pcall(function()
                    if string.find(obj.Name, name) or obj.Name == name then
                        obj:Destroy()
                        deletedCount = deletedCount + 1
                    end
                end)
            end
        end
    end

    -- List of all services to scan (Combined from both scripts)
    local services = {
        game:GetService("Workspace"),
        game:GetService("Lighting"),
        game:GetService("ReplicatedStorage"),
        game:GetService("ReplicatedFirst"),
        game:GetService("StarterPack"),
        game:GetService("ServerStorage"), -- Note: Usually nil on client, but kept for consistency
        game:GetService("ServerScriptService"), -- Note: Usually nil on client
        game:GetService("SoundService"),
        game:GetService("Chat"),
        game:GetService("LocalizationService"),
        game:GetService("TestService"),
        game:GetService("Teams"),
        game:GetService("Debris"),
        LocalPlayer:WaitForChild("PlayerScripts", 5),
        LocalPlayer:WaitForChild("PlayerGui", 5),
        LocalPlayer:WaitForChild("Backpack", 5),
        LocalPlayer -- Scans the Player object itself
    }

    -- Execute scan across all services
    for _, service in ipairs(services) do
        scanAndDestroy(service)
    end

    -- Anti-AFK Logic (from Script 1)
    pcall(function()
        LocalPlayer.Idled:Connect(function()
            local vu = game:GetService("VirtualUser")
            vu:Button2Down(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
            task.wait(1)
            vu:Button2Up(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
        end)
    end)


    -- Feedback: Reliable Notification (from Script 2)
    task.spawn(function()
        local success = false
        local attempts = 0
        while not success and attempts < 10 do
            success = pcall(function()
                StarterGui:SetCore("SendNotification", {
                    Title = "Bypass Active",
                    Text = "Removed " .. deletedCount .. " elements.",
                    Duration = 5
                })
            end)
            if not success then 
                attempts = attempts + 1
                task.wait(1) 
            end
        end
    end)
end

-- Execute the combined script
destroyAntiCheat()
end)
