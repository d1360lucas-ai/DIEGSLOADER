local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")

local player = Players.LocalPlayer
local PlayerGui = player:WaitForChild("PlayerGui")

local old = PlayerGui:FindFirstChild("MonsyLoader")
if old then
	old:Destroy()
end

local gui = Instance.new("ScreenGui")
gui.Name = "DiegsLoader"
gui.ResetOnSpawn = false
gui.IgnoreGuiInset = true
gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
gui.DisplayOrder = 50
gui.Parent = PlayerGui

local function corner(parent, radius)
	local c = Instance.new("UICorner")
	c.CornerRadius = UDim.new(0, radius or 8)
	c.Parent = parent
	return c
end

local function stroke(parent, color, thickness)
	local s = Instance.new("UIStroke")
	s.Color = color or Color3.fromRGB(45,45,45)
	s.Thickness = thickness or 1
	s.Parent = parent
	return s
end

local function makeLabel(parent, text, size, color)
	local l = Instance.new("TextLabel")
	l.BackgroundTransparency = 1
	l.Text = text
	l.TextSize = size or 14
	l.Font = Enum.Font.Code
	l.TextColor3 = color or Color3.fromRGB(220,220,220)
	l.TextXAlignment = Enum.TextXAlignment.Left
	l.TextYAlignment = Enum.TextYAlignment.Center
	l.Parent = parent
	return l
end

-- MAIN
local main = Instance.new("Frame")
main.Name = "Main"
main.AnchorPoint = Vector2.new(1,0.5)
main.Position = UDim2.new(1,-25,0.5,0)
main.Size = UDim2.fromOffset(330,520)
main.BackgroundColor3 = Color3.fromRGB(3,3,3)
main.BorderSizePixel = 0
main.Active = true
main.Parent = gui

corner(main,10)
stroke(main,Color3.fromRGB(48,48,48),1)

-- HEADER
local header = Instance.new("Frame")
header.Size = UDim2.new(1,0,0,72)
header.BackgroundTransparency = 1
header.Parent = main

local title = makeLabel(
	header,
	"DIEGS LOADER",
	14,
	Color3.fromRGB(235,235,235)
)
title.Position = UDim2.fromOffset(16,10)
title.Size = UDim2.new(1,-60,0,24)

local author = makeLabel(
	header,
	"@ qwdgo",
	9,
	Color3.fromRGB(105,105,105)
)
author.Position = UDim2.fromOffset(17,34)
author.Size = UDim2.new(1,-34,0,16)

-- X BUTTON
local close = Instance.new("TextButton")
close.Name = "CloseButton"
close.AnchorPoint = Vector2.new(1,0)
close.Position = UDim2.new(1,-9,0,8)
close.Size = UDim2.fromOffset(28,28)
close.BackgroundTransparency = 1
close.Text = "×"
close.TextSize = 23
close.Font = Enum.Font.Code
close.TextColor3 = Color3.fromRGB(120,120,120)
close.AutoButtonColor = false
close.ZIndex = 20
close.Parent = header

close.MouseEnter:Connect(function()
	close.TextColor3 = Color3.fromRGB(255,255,255)
end)

close.MouseLeave:Connect(function()
	close.TextColor3 = Color3.fromRGB(120,120,120)
end)

close.MouseButton1Click:Connect(function()
	gui:Destroy()
end)

local line = Instance.new("Frame")
line.Position = UDim2.fromOffset(14,55)
line.Size = UDim2.new(1,-28,0,1)
line.BackgroundColor3 = Color3.fromRGB(55,55,55)
line.BorderSizePixel = 0
line.Parent = header

-- MODULE LIST
local scroll = Instance.new("ScrollingFrame")
scroll.Name = "Modules"
scroll.Position = UDim2.fromOffset(14,66)
scroll.Size = UDim2.new(1,-28,1,-94)
scroll.BackgroundTransparency = 1
scroll.BorderSizePixel = 0
scroll.ScrollBarThickness = 2
scroll.ScrollBarImageColor3 = Color3.fromRGB(80,80,80)
scroll.CanvasSize = UDim2.new()
scroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
scroll.Parent = main

local padding = Instance.new("UIPadding")
padding.PaddingTop = UDim.new(0,6)
padding.PaddingBottom = UDim.new(0,5)
padding.Parent = scroll

local layout = Instance.new("UIListLayout")
layout.Padding = UDim.new(0,5)
layout.SortOrder = Enum.SortOrder.LayoutOrder
layout.Parent = scroll

local modules = {
	{"DIEGS MENU","YAN LANG MUNA"},
	{"COMING SOON","WAG MAKULET"},
	{"COMING SOON","WAG MAKULET"},
	{"COMING SOON","WAG MAKULET"},
	{"COMING SOON","WAG MAKULET"},
	{"COMING SOON","WAG MAKULET"},
}

-- DIEGS POPUP
local function openDIEGSMenu()

	local existing = gui:FindFirstChild("DIEGSPopup")
	if existing then
		existing:Destroy()
	end

	local popup = Instance.new("Frame")
	popup.Name = "DIEGSPopup"
	popup.Size = UDim2.fromOffset(310,430)
	popup.AnchorPoint = Vector2.new(0.5,0.5)
	popup.Position = UDim2.new(0.5,0,0.5,0)
	popup.BackgroundColor3 = Color3.fromRGB(2,2,2)
	popup.BorderSizePixel = 0
	popup.Active = true
	popup.ZIndex = 100
	popup.Parent = gui

	corner(popup,12)
	stroke(popup,Color3.fromRGB(50,50,50),1)

	-- TITLE
	local popupTitle = makeLabel(
		popup,
		"// DIEGS MENU @ coded by qwdgo",
		14,
		Color3.fromRGB(255,255,255)
	)
	popupTitle.Position = UDim2.fromOffset(18,8)
	popupTitle.Size = UDim2.new(1,-65,0,38)
	popupTitle.ZIndex = 101

	-- CLOSE
	local popupClose = Instance.new("TextButton")
	popupClose.Size = UDim2.fromOffset(35,35)
	popupClose.Position = UDim2.new(1,-43,0,6)
	popupClose.BackgroundTransparency = 1
	popupClose.Text = "X"
	popupClose.TextColor3 = Color3.fromRGB(150,150,150)
	popupClose.TextSize = 18
	popupClose.Font = Enum.Font.Code
	popupClose.AutoButtonColor = false
	popupClose.ZIndex = 110
	popupClose.Parent = popup

	popupClose.MouseEnter:Connect(function()
		popupClose.TextColor3 = Color3.fromRGB(255,255,255)
	end)

	popupClose.MouseLeave:Connect(function()
		popupClose.TextColor3 = Color3.fromRGB(150,150,150)
	end)

	popupClose.MouseButton1Click:Connect(function()
		popup:Destroy()
	end)

	local popupLine = Instance.new("Frame")
	popupLine.Position = UDim2.fromOffset(15,47)
	popupLine.Size = UDim2.new(1,-30,0,1)
	popupLine.BackgroundColor3 = Color3.fromRGB(50,50,50)
	popupLine.BorderSizePixel = 0
	popupLine.ZIndex = 101
	popupLine.Parent = popup

	-- CONTENT
	local content = Instance.new("ScrollingFrame")
	content.Position = UDim2.fromOffset(15,58)
	content.Size = UDim2.new(1,-30,1,-68)
	content.BackgroundTransparency = 1
	content.BorderSizePixel = 0
	content.ScrollBarThickness = 2
	content.ScrollBarImageColor3 = Color3.fromRGB(80,80,80)
	content.CanvasSize = UDim2.new()
	content.AutomaticCanvasSize = Enum.AutomaticSize.Y
	content.ZIndex = 101
	content.Parent = popup

	local contentLayout = Instance.new("UIListLayout")
	contentLayout.Padding = UDim.new(0,7)
	contentLayout.SortOrder = Enum.SortOrder.LayoutOrder
	contentLayout.Parent = content

	local buttonNames = {
		"REF ALL CITY",
		"DISTRICT SPAWN GUN",
		"SYNCED SPAWN GUN",
		"SYNCED SPAWN CASH",
		"SYNCED SPAWN DIRTY CASH",
		"SYNCED SPAWN CAR",
		"SAN ANDREAS SPAWN CASH",
		"SAN ANDREAS CRASH",
	}

	for i,name in ipairs(buttonNames) do

		local button = Instance.new("TextButton")
		button.Name = name:gsub("%s","")
		button.Size = UDim2.new(1,0,0,38)
		button.BackgroundColor3 = Color3.fromRGB(18,18,18)
		button.BorderSizePixel = 0
		button.Text = name
		button.TextColor3 = Color3.fromRGB(225,225,225)
		button.TextSize = 10
		button.Font = Enum.Font.Code
		button.AutoButtonColor = false
		button.LayoutOrder = i
		button.ZIndex = 102
		button.Parent = content

		corner(button,7)
		stroke(button,Color3.fromRGB(38,38,38),1)

		button.MouseEnter:Connect(function()
			button.BackgroundColor3 = Color3.fromRGB(30,30,30)
		end)

		button.MouseLeave:Connect(function()
			button.BackgroundColor3 = Color3.fromRGB(18,18,18)
		end)

		-- BUTTON ACTION
		button.MouseButton1Click:Connect(function()

			if name == "REF ALL CITY" then

				local args = {
					[1] = {
						["FirstName"] = "x",
						["Gender"] = "Boy",
						["LastName"] = "x",
						["DOB"] = "30-10-2005",
					}
				}

				game:GetService("ReplicatedStorage")
					:WaitForChild("SubmitIdentity")
					:FireServer(unpack(args))

				print("REF ALL CITY executed")

			else
				print(name .. " clicked")
			end

			if name == "DISTRICT SPAWN GUN" then

local args = {
    [1] = "Deagle";
}

game:GetService("ReplicatedStorage"):WaitForChild("PoliceToolEvent", 9e9):WaitForChild("ToggleToolEvent", 9e9):FireServer(unpack(args))

				print("DISTRICT SPAWN GUN executed")

			else
				print(name .. " clicked")
			end

			if name == "SYNCED SPAWN GUN" then

local args = {
    [1] = "Combat Pistol";
    [2] = -1;
    [3] = "roytjdf755875";
}

game:GetService("ReplicatedStorage"):WaitForChild("XYANFOLDER", 9e9):WaitForChild("LoadingScreenEvent", 9e9):FireServer(unpack(args))

				print("SYNCED SPAWN GUN executed")

			else
				print(name .. " clicked")
			end

			if name == "SYNCED SPAWN CASH" then

local Event = game:GetService("ReplicatedStorage").XYANFOLDER.LoadingScreenEvent
Event:FireServer(
    "Red Pill",
    -10000000,
    "roytjdf755875"
)

				print("SYNCED SPAWN CASH executed")

			else
				print(name .. " clicked")
			end

			if name == "SYNCED SPAWN DIRTY CASH" then

game:GetService("ReplicatedStorage"):FindFirstChild("XYANFOLDER"):FindFirstChild("Blackmarket"):FireServer(".50", -1000000)

				print("SYNCED SPAWN DIRTY CASH executed")

			else
				print(name .. " clicked")
			end

			if name == "SYNCED SPAWN CAR" then

local Event = game:GetService("ReplicatedStorage").DealerSystem.Remotes.SpawnVehicle
Event:FireServer(
    "SupraMk5"
)

				print("SYNCED SPAWN CAR executed")

			else
				print(name .. " clicked")
			end

			if name == "SAN ANDREAS SPAWN CASH" then

game:GetService("ReplicatedStorage").NikkiiBlackmarketFolder.BuyBm:FireServer("Pill", -100000000)

				print("SAN ANDREAS SPAWN CASH executed")

			else
				print(name .. " clicked")
			end
		end)
	end

	-- DRAG
	local dragging = false
	local dragStart
	local startPosition

	popupTitle.InputBegan:Connect(function(input)

		if input.UserInputType == Enum.UserInputType.MouseButton1
			or input.UserInputType == Enum.UserInputType.Touch then

			dragging = true
			dragStart = input.Position
			startPosition = popup.Position

			input.Changed:Connect(function()
				if input.UserInputState == Enum.UserInputState.End then
					dragging = false
				end
			end)
		end
	end)

	UserInputService.InputChanged:Connect(function(input)

		if not dragging then
			return
		end

		if input.UserInputType == Enum.UserInputType.MouseMovement
			or input.UserInputType == Enum.UserInputType.Touch then

			local delta = input.Position - dragStart

			popup.Position = UDim2.new(
				startPosition.X.Scale,
				startPosition.X.Offset + delta.X,
				startPosition.Y.Scale,
				startPosition.Y.Offset + delta.Y
			)
		end
	end)
end

-- CREATE MODULES
local function createModule(index,moduleName,description)

	local row = Instance.new("Frame")
	row.Name = "Module_" .. index
	row.Size = UDim2.new(1,0,0,65)
	row.BackgroundColor3 = Color3.fromRGB(3,3,3)
	row.BorderSizePixel = 0
	row.LayoutOrder = index
	row.Parent = scroll

	local number = makeLabel(
		row,
		tostring(index)..".",
		9,
		Color3.fromRGB(105,105,105)
	)

	number.Position = UDim2.fromOffset(0,9)
	number.Size = UDim2.fromOffset(24,18)
	number.TextXAlignment = Enum.TextXAlignment.Center

	local name = makeLabel(
		row,
		moduleName,
		11,
		Color3.fromRGB(225,225,225)
	)

	name.Position = UDim2.fromOffset(28,7)
	name.Size = UDim2.new(1,-105,0,20)

	local sub = makeLabel(
		row,
		description,
		8,
		Color3.fromRGB(88,88,88)
	)

	sub.Position = UDim2.fromOffset(28,30)
	sub.Size = UDim2.new(1,-105,0,16)

	local load = Instance.new("TextButton")
	load.Name = "Load"
	load.AnchorPoint = Vector2.new(1,0.5)
	load.Position = UDim2.new(1,-1,0.5,0)
	load.Size = UDim2.fromOffset(65,28)
	load.BackgroundColor3 = Color3.fromRGB(0,0,0)
	load.BorderSizePixel = 0
	load.Text = "LOAD"
	load.TextSize = 9
	load.Font = Enum.Font.Code
	load.TextColor3 = Color3.fromRGB(220,220,220)
	load.AutoButtonColor = false
	load.Parent = row

	corner(load,7)
	stroke(load,Color3.fromRGB(15,15,15),1)

	load.MouseEnter:Connect(function()
		load.BackgroundColor3 = Color3.fromRGB(18,18,18)
	end)

	load.MouseLeave:Connect(function()
		load.BackgroundColor3 = Color3.fromRGB(0,0,0)
	end)

	load.MouseButton1Click:Connect(function()

		if moduleName == "DIEGS MENU" then
			openDIEGSMenu()
		else
			print(moduleName.." selected")
		end

	end)
end

for i,data in ipairs(modules) do
	createModule(i,data[1],data[2])
end

-- FOOTER
local footer = makeLabel(
	main,
	"discord.gg/qwdgo",
	8,
	Color3.fromRGB(70,70,70)
)

footer.AnchorPoint = Vector2.new(0.5,1)
footer.Position = UDim2.new(0.5,0,1,-3)
footer.Size = UDim2.new(1,-20,0,16)
footer.TextXAlignment = Enum.TextXAlignment.Center

-- DRAG MAIN
local dragging = false
local dragStart
local startPosition

header.InputBegan:Connect(function(input)

	if input.UserInputType == Enum.UserInputType.MouseButton1
		or input.UserInputType == Enum.UserInputType.Touch then

		dragging = true
		dragStart = input.Position
		startPosition = main.Position

		input.Changed:Connect(function()
			if input.UserInputState == Enum.UserInputState.End then
				dragging = false
			end
		end)
	end
end)

UserInputService.InputChanged:Connect(function(input)

	if not dragging then
		return
	end

	if input.UserInputType == Enum.UserInputType.MouseMovement
		or input.UserInputType == Enum.UserInputType.Touch then

		local delta = input.Position - dragStart

		main.Position = UDim2.new(
			startPosition.X.Scale,
			startPosition.X.Offset + delta.X,
			startPosition.Y.Scale,
			startPosition.Y.Offset + delta.Y
		)
	end
end)
