local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local Players = game:GetService("Players")

local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- 1. Main ScreenGui
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "QwdgoMenuGui"
screenGui.ResetOnSpawn = false
screenGui.Parent = playerGui

-- 2. Main Frame
local mainFrame = Instance.new("Frame")
mainFrame.Name = "MainFrame"
mainFrame.Size = UDim2.new(0, 260, 0, 360)
mainFrame.Position = UDim2.new(0.5, -130, 0.5, -180)
mainFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
mainFrame.BorderSizePixel = 0
mainFrame.Active = true
mainFrame.Parent = screenGui

local mainUICorner = Instance.new("UICorner")
mainUICorner.CornerRadius = UDim.new(0, 12)
mainUICorner.Parent = mainFrame

-- Title Button (Clickable para sa Hide/Show)
local titleBtn = Instance.new("TextButton")
titleBtn.Name = "TitleButton"
titleBtn.Size = UDim2.new(1, -40, 0, 30)
titleBtn.Position = UDim2.new(0, 10, 0, 8)
titleBtn.BackgroundTransparency = 1
titleBtn.Text = "HIRAYA MENU"
titleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
titleBtn.TextSize = 14
titleBtn.Font = Enum.Font.Code
titleBtn.TextXAlignment = Enum.TextXAlignment.Left
titleBtn.Parent = mainFrame

-- Subtitle
local subLabel = Instance.new("TextLabel")
subLabel.Name = "Subtitle"
subLabel.Size = UDim2.new(1, 0, 0, 15)
subLabel.Position = UDim2.new(0, 10, 0, 32)
subLabel.BackgroundTransparency = 1
subLabel.Text = "// @ coded by qwdgo"
subLabel.TextColor3 = Color3.fromRGB(120, 120, 120)
subLabel.TextSize = 10
subLabel.Font = Enum.Font.Code
subLabel.TextXAlignment = Enum.TextXAlignment.Left
subLabel.Parent = mainFrame

-- Close Button (X)
local closeBtn = Instance.new("TextButton")
closeBtn.Name = "CloseButton"
closeBtn.Size = UDim2.new(0, 25, 0, 25)
closeBtn.Position = UDim2.new(1, -30, 0, 8)
closeBtn.BackgroundTransparency = 1
closeBtn.Text = "X"
closeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
closeBtn.TextSize = 14
closeBtn.Font = Enum.Font.Code
closeBtn.Parent = mainFrame

closeBtn.MouseButton1Click:Connect(function()
	screenGui:Destroy()
end)

-- 3. Scrolling Frame para sa mga Button
local scrollFrame = Instance.new("ScrollingFrame")
scrollFrame.Name = "ButtonsContainer"
scrollFrame.Size = UDim2.new(1, -20, 1, -65)
scrollFrame.Position = UDim2.new(0, 10, 0, 55)
scrollFrame.BackgroundTransparency = 1
scrollFrame.BorderSizePixel = 0
scrollFrame.ScrollBarThickness = 4
scrollFrame.ScrollBarImageColor3 = Color3.fromRGB(80, 80, 80)
scrollFrame.Parent = mainFrame

local listLayout = Instance.new("UIListLayout")
listLayout.Padding = UDim.new(0, 8)
listLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
listLayout.SortOrder = Enum.SortOrder.LayoutOrder
listLayout.Parent = scrollFrame

-- Function para gumawa ng Button
local function createButton(text, order)
	local btn = Instance.new("TextButton")
	btn.Name = text:gsub("%s+", "") .. "Btn"
	btn.Size = UDim2.new(1, -8, 0, 42)
	btn.BackgroundColor3 = Color3.fromRGB(28, 28, 28)
	btn.BorderSizePixel = 0
	btn.Text = text
	btn.TextColor3 = Color3.fromRGB(220, 220, 220)
	btn.TextSize = 12
	btn.Font = Enum.Font.Code
	btn.LayoutOrder = order
	btn.Parent = scrollFrame

	local btnCorner = Instance.new("UICorner")
	btnCorner.CornerRadius = UDim.new(0, 8)
	btnCorner.Parent = btn

	return btn
end

-- Paggawa ng mga Button
local btnTPBase = createButton("TP TO BASE", 1)
local btnG20 = createButton("BUY G20 GUN", 2)
local btnCapsul = createButton("BUY CAPSUL", 3)
local btnFood = createButton("BUY FOOD", 4)
local btnWater = createButton("BUY WATER", 5)
local btnWithdraw = createButton("WITHDRAW 50K", 6)

-- Auto Canvas Size adjustment
scrollFrame.CanvasSize = UDim2.new(0, 0, 0, listLayout.AbsoluteContentSize.Y + 10)
listLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
	scrollFrame.CanvasSize = UDim2.new(0, 0, 0, listLayout.AbsoluteContentSize.Y + 10)
end)

-- ============================================================
-- HIDE / SHOW TOGGLE LOGIC
-- ============================================================
local isExpanded = true
local expandedSize = UDim2.new(0, 260, 0, 360)
local minimizedSize = UDim2.new(0, 260, 0, 50)

titleBtn.MouseButton1Click:Connect(function()
	isExpanded = not isExpanded
	
	-- I-animate ang laki ng menu frame
	local targetSize = isExpanded and expandedSize or minimizedSize
	local tweenInfo = TweenInfo.new(0.3, Enum.EasingStyle.Quart, Enum.EasingDirection.Out)
	
	TweenService:Create(mainFrame, tweenInfo, {Size = targetSize}):Play()
	
	-- Itago o ipakita ang container ng mga button
	scrollFrame.Visible = isExpanded
end)

-- Dragging Functionality
local dragging, dragInput, dragStart, startPos
mainFrame.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
		dragging = true
		dragStart = input.Position
		startPos = mainFrame.Position
		input.Changed:Connect(function()
			if input.UserInputState == Enum.UserInputState.End then
				dragging = false
			end
		end)
	end
end)

mainFrame.InputChanged:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
		dragInput = input
	end
end)

UserInputService.InputChanged:Connect(function(input)
	if input == dragInput and dragging then
		local delta = input.Position - dragStart
		mainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
	end
end)

-- ============================================================
-- FUNCTION NG MGA BUTTON:
-- ============================================================

btnTPBase.MouseButton1Click:Connect(function()
	    local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()

local target = workspace["Gang Base"]["LCF X ESP"]

if target:IsA("BasePart") then
    character:PivotTo(target.CFrame + Vector3.new(0, 3, 0))
elseif target:IsA("Model") then
    character:PivotTo(target:GetPivot() + Vector3.new(0, 3, 0))
end

end)

btnG20.MouseButton1Click:Connect(function()
    local Event = game:GetService("ReplicatedStorage").ShopEvent.RequestBuy
Event:FireServer(
    "Product3"
)

end)

btnCapsul.MouseButton1Click:Connect(function()
    local Event = game:GetService("ReplicatedStorage").ShopEvent.RequestBuy
Event:FireServer(
    "Product5"
)

end)

btnFood.MouseButton1Click:Connect(function()
    local Event = game:GetService("ReplicatedStorage").ShopEvent["711RequestBuy"]
Event:FireServer(
    "Product2"
)

end)

btnWater.MouseButton1Click:Connect(function()
    local Event = game:GetService("ReplicatedStorage").ShopEvent["711RequestBuy"]
Event:FireServer(
    "Product1"
)

end)

btnWithdraw.MouseButton1Click:Connect(function()
    local Event = game:GetService("ReplicatedStorage").BankEvent.BankService
Event:InvokeServer(
    "Withdraw",
    {
        Username = "",
        Amount = "50000"
    }
)

end)
